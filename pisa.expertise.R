# updateCheckboxGroupInput(session, inputId="Performers", label="רמת בקיאות", inline=T, choices = c(
#   "מצטיינים"="High",
#   "מתקשים"="Low"
# ), selected = NULL)

# column(3,
#        checkboxGroupInput(inputId="Performers", label="רמת בקיאות", inline=T, choices = c(
#          "מצטיינים"="High",
#          "מתקשים"="Low"
#        ))
# ),